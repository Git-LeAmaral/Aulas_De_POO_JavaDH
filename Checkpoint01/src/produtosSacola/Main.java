package produtosSacola;

import java.util.Scanner;
/*
public class Main {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        SacolaDeCompras sacola = new SacolaDeCompras();

        boolean continuarComprando = true;
        while (continuarComprando) {
            System.out.println("Digite o nome do produto: ");
            String nome = scanner.nextLine();

            System.out.println("Digite o preço do produto: ");
            double preco = scanner.nextDouble();

            System.out.println("Digite a quantidade: ");
            int quantidade = scanner.nextInt();

            ProdutoSa produto = new ProdutoSa(nome, preco, quantidade);
            sacola.adicionarProduto(produto);

            System.out.println("Deseja continuar comprando? (S/N)");
            String resposta = scanner.next();
            continuarComprando = resposta.equalsIgnoreCase("S");

            scanner.nextLine(); // Limpar o buffer
        }

        // Exibir os produtos na sacola
        System.out.println("Produtos na sacola de compras:");
        for (ProdutoSa produto : sacola.getProdutos()) {
            System.out.println(produto.getNome() + " - " + produto.getPreco() + " - " + produto.getQuantidade());
        }

        // Outras operações com a sacola de compras aqui
    }

}
*/